import React from 'react'

function Card(props) {
    const info = props;
    return (
        <div className="individual-card">
            <img src='' ></img>
            <p></p>
        </div>
    )
}

export default Card;
